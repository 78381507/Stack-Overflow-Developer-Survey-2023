def explode_multiselect(df, column):
    return (
        df[column]
        .dropna()
        .str.split(";")
        .explode()
        .str.strip()
        .value_counts()
        .reset_index()
        .rename(columns={"index": column, column: "count"})
    )
